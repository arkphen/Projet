/**
 * Created by acharland on 11/29/16.
 */
// Include http module,
var http = require("http"),
// And mysql module you've just installed.
    fs = require("fs");

// Create the http server.
http.createServer(function (request, response) {
    connection
    (
        if(url)
        {
            querry
        }
    )
    });
// Listen on the 8080 port.
}).listen(8080);